# Per-application keyd overrides
# Emacs: keep Super (Meta) as-is, don't remap to Ctrl
[Emacs]
# empty = no overrides, use global keyd config
# Emacs will receive Super+key (s-key) instead of C-key

[emacs]
# lowercase variant just in case

